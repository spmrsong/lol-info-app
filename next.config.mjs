/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ddragon.leagueoflegends.com",
      "cmsassets.rgpub.io",
      "https://lol-info-app-1st.vercel.app",
    ],
  },
};

export default nextConfig;
